<Window
    x:Class="SafeRun.WPFCtrlEquip.WPFRunner.MainFrm"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SafeRun.WPFCtrlEquip.WPFRunner"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    Title="业务监控系统"
    Width="800"
    Height="450"
    mc:Ignorable="d" Icon="/SafeRun.WPFCtrlEquip.WPFRunner;component/View/SafeRunLogon.ico" >
    <Window.Resources>
        <Style x:Key="defaultRadioStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="Foreground" Value="#88949A" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid>
                            <Border x:Name="border" Margin="20,0,20,0" />
                            <ContentPresenter
                                x:Name="contentPresenter"
                                Grid.Column="1"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                Focusable="False"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="#8CEDCB" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#8CEDCB" />
                                <Setter TargetName="border" Property="BorderThickness" Value="0,0,0,4" />
                            </Trigger>

                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="border" Property="BorderThickness" Value="0" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <!--<RowDefinition Height="75" />-->
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <!--  标题和工具栏  --><!--
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Horizontal">
                <Border
                    Width="30"
                    Height="30"
                    Background="#59E6B5"
                    CornerRadius="30" />
                <Border
                    Width="30"
                    Height="30"
                    Margin="-20,0,0,0"
                    Background="#FF7900"
                    CornerRadius="30" />
                <TextBlock
                    Margin="5,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="16"
                    FontWeight="Bold"
                    Foreground="#4F5050"
                    Text="SafeRun" />
            </StackPanel>

            <UniformGrid Grid.Column="1" Columns="5">
                <RadioButton Style="{StaticResource defaultRadioStyle}" Checked="RadioButton_Checked">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="Fonts/#iconfont"
                            FontSize="18"
                            Text="&#xe602;" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="首页" />
                    </StackPanel>
                </RadioButton>

                <RadioButton Style="{StaticResource defaultRadioStyle}" Visibility="Hidden">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="Fonts/#iconfont"
                            FontSize="18"
                            Text="&#xe647;" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Apps" />
                    </StackPanel>
                </RadioButton>

                <RadioButton Style="{StaticResource defaultRadioStyle}" Visibility="Hidden">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="Fonts/#iconfont"
                            FontSize="18"
                            Text="&#xe6a5;" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Pages" />
                    </StackPanel>
                </RadioButton>

                <RadioButton Style="{StaticResource defaultRadioStyle}" Visibility="Hidden">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="Fonts/#iconfont"
                            FontSize="18"
                            Text="&#xe623;" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="UI Kit" />
                    </StackPanel>
                </RadioButton>

                <RadioButton Style="{StaticResource defaultRadioStyle}" Visibility="Hidden">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock
                            VerticalAlignment="Center"
                            FontFamily="Fonts/#iconfont"
                            FontSize="18"
                            Text="&#xe649;" />
                        <TextBlock
                            Margin="10,0,0,0"
                            VerticalAlignment="Center"
                            Text="Forms" />
                    </StackPanel>
                </RadioButton>

            </UniformGrid>

        </Grid>-->

        <!--  头像区域  -->
        <StackPanel
            Grid.Column="1"
            HorizontalAlignment="Center"
            Orientation="Horizontal" />

        <!--  右侧用户栏  -->
        <Grid Grid.Row="1" Grid.Column="1" />

        <!--  主体内容-横跨两个  -->
        <Grid
            x:Name="StackPanelMain"
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Background="#F5F6FA">
            <ContentControl x:Name="mainContent" Margin="0,0,0,0" />
        </Grid>
    </Grid>
</Window>
